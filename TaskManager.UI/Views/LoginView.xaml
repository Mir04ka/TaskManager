<UserControl x:Class="TaskManager.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Margin="10" Width="300">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <TextBlock Text="🌐" FontSize="16" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox ItemsSource="{Binding LanguageVM.AvailableLanguages}"
                      SelectedItem="{Binding LanguageVM.SelectedLanguage}"
                      Width="60"/>
        </StackPanel>
        
        <TextBlock Text="{DynamicResource Username}" />
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="{DynamicResource Password}" Margin="0,8,0,0" />
        <PasswordBox x:Name="PwdBox" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="{DynamicResource Login}" Width="140" Height="50" Margin="0,0,5,0"
                    Click="LoginButtonClick" />
            <Button Content="{DynamicResource Register}" Width="140" Height="50" Margin="5,0,0,0"
                    Click="RegisterButtonClick" />
        </StackPanel>

        <TextBlock Text="{Binding StatusMessage}" Foreground="Red" Margin="0,8,0,0"/>
    </StackPanel>
</UserControl>