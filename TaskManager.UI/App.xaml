<Application x:Class="TaskManager.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TaskManager.UI.ViewModels"
             xmlns:views="clr-namespace:TaskManager.UI.Views">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Blue.xaml" />
                <ResourceDictionary Source="Resources/Styles.xaml" />
                <ResourceDictionary Source="Localization/Strings.en.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters -->
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
            <views:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
            
            <DataTemplate DataType="{x:Type vm:LoginViewModel}">
                <views:LoginView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:TaskViewModel}">
                <views:TaskView/>
            </DataTemplate>
            
            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <StackPanel Margin="20">
                    <TextBlock Text="{DynamicResource Settings}" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                    <TextBlock Text="{DynamicResource Language}"/>
                    <ComboBox ItemsSource="{Binding AvailableLanguages}"
                              SelectedItem="{Binding SelectedLanguage}"
                              Width="200" HorizontalAlignment="Left" Margin="0,5,0,20"/>
                    <Button Command="{Binding DataContext.ShowTasksCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            Width="150" HorizontalAlignment="Left">
                        <TextBlock Text="{DynamicResource BackToTasks}"/>
                    </Button>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>